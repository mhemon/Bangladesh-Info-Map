{
  "name": "bd-maps",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:proxy": "node scripts/api-proxy-server.js",
    "build": "vite build",
    "preview": "vite preview",
    "build:data": "node scripts/build-real-dataset.js",
    "build:data:allow-missing": "node scripts/build-real-dataset.js --allow-missing",
    "build:data:wikidata": "node scripts/fetch-real-from-wikidata.js",
    "validate:districts": "node -e \"import('./scripts/district-name-validator.js').then(m=>console.log('Canonical districts:',m.CANONICAL_DISTRICTS.length))\""
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0",
    "xlsx": "^0.18.5"
  }
}
